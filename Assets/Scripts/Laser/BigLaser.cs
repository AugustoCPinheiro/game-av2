using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BigLaser : Laser
{
    // Start is called before the first frame update
    void Start()
    {
        _damage = 2;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
